# dsh-otel-plugin v0.1.3

## Fixed

- Align `install.sh` and `install-release.sh` with the standard external-plugin installer flow used by OpenClaw and Hermes.
- Accept the connector's generic `--type gtrace` argument.
- Resolve and verify the published GitHub archive automatically when no source is supplied.
- Keep Unix and Windows release installers consistent.
